<CustomCheckBox>:
    color: app.theme_cls.primary_color if self.active else app.theme_cls.disabled_color
    icon: self.checkbox_icon_down if self.active else self.checkbox_icon_normal
    size: "40dp", "40dp"
    text_size: self.size
    halign: "center"
    valign: "center"
